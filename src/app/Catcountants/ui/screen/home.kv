#:kivy 2.3.1

#:import dp kivy.metrics.dp
#:import MDScreen kivymd.uix.screen
#:import MDStackLayout kivymd.uix.stacklayout
#:import MDLabel kivymd.uix.label
#:import CatBasePrimaryScreen ui.screen.baseprimary
#:import BalancePanel ui.component.panel

<HomeScreen@CatBasePrimaryScreen>:
    name: "home"
    title: "首頁"
    
    MDStackLayout:
        orientation: "tb-lr"
        padding: dp(20), dp(20), dp(20), dp(28 + 20)
        size_hint_y: None
        pos_hint: {"top": 1}

        BalancePanel:
